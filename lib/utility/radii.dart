import 'package:flutter/cupertino.dart';

class Radii {
  static const BorderRadiusGeometry k15pxRadius = BorderRadius.all(Radius.circular(15.5));
  static const BorderRadiusGeometry k11pxRadius = BorderRadius.all(Radius.circular(11));
  static const BorderRadiusGeometry k5pxRadius =BorderRadius.all(Radius.circular(5));
  static const BorderRadiusGeometry k8pxRadius =BorderRadius.all(Radius.circular(8));
}